kind: Service
apiVersion: v1
metadata:
  name:  db-lb-b
  labels:
    app: db-lb-b
spec:
  selector:
    app:  db-b
  type:  LoadBalancer
  ports:
  - protocol: "TCP"
    # Port accessible inside cluster
    port: 3306
    # Port to forward to inside the pod
    targetPort: 3306
    # Port accessible outside cluster
    nodePort: 30202